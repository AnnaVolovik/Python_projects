from setuptools import setup
setup(
    name = 'just_another_parser_v1',
    packages = ['just_another_parser_v1'],
    include_package_data = True,
    install_requires = [
        'flask',
        ],
    )
